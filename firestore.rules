// firestore.rules (completo, con permisos para admins y guard)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /chatMessages/{docId} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if false;
    }

    match /ranking/{uid} {
      allow read: if true;
      allow create, update: if request.auth != null
                              && request.auth.uid == uid
                              && request.resource.data.uid == uid;
      allow delete: if false;
    }

    match /resultados/{docId} {
      allow read: if true;
      allow create: if request.auth != null
                      && request.resource.data.uid == request.auth.uid;
      allow update, delete: if false;
    }

    // Encuestas: creación para usuarios logueados. Lectura y borrado SOLO para admins.
    match /encuestas/{docId} {
      allow create: if request.auth != null
                      && request.resource.data.uid == request.auth.uid;

      allow read, delete: if request.auth != null && (
        request.auth.token.admin == true ||
        exists(/databases/$(database)/documents/admins/$(request.auth.uid))
      );

      allow update: if false;
    }

    // Colección de admins (usada por el Auth Guard y las reglas de seguridad)
    match /admins/{uid} {
      // El propio usuario puede leer su propio documento de admin (lo usa el guard)
      allow read: if request.auth != null && request.auth.uid == uid;
      // La creación, actualización y borrado de admins se gestiona desde la consola/scripts.
      allow create, update, delete: if false;
    }

    match /loginLogs/{docId} {
      allow create: if request.auth != null
                      && request.resource.data.uid == request.auth.uid;
      allow read, update, delete: if false;
    }
  }
}
